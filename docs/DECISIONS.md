# DECISIONS

Anything that contradicts or extends the specs goes here, with a date and a
reason. If code and docs disagree, this file is where the disagreement gets
resolved — and then the spec gets updated.

| Date | Decision | Why | Docs updated |
|---|---|---|---|
| 2026-08-23 | Next.js + MUI + Supabase, no ORM, no state library | Fewest moving parts for a single-user app; MUI gives Material 3 without hand-rolling components; Supabase gives auth + Postgres + RLS in one. | `00-CONTEXT.md §3` |
| 2026-08-23 | All program logic lives in pure `src/core` | The generator is the product; it must be testable in milliseconds without a database. | `03-ARCHITECTURE.md §1` |
| 2026-08-23 | Balance rules B1–B10 are hard constraints, not heuristics | "Look good, move well" only survives contact with a generator if it is enforced by validation. | `01-METHODOLOGY.md §4.4` |
| 2026-08-23 | Time budget trims accessories, never the main lift | The main part stays big as hell by construction. | `01-METHODOLOGY.md §1.3, §6.3` |
| 2026-08-24 | Balance ratios are validated on week one, not every week | Later weeks re-materialise from week one, so the movements are identical; only the wave changes set counts. Checking ratios weekly made the peak week (which has a top set and deliberately less accessory volume) fail a structural rule it cannot control. Weeks 2+ are checked on invariants instead: one main lift per session, a weekly carry, 48-hour spacing, under the cap. | `01-METHODOLOGY.md §4.4` |
| 2026-08-24 | Trim ladder drops secondary sets before the finisher | The plan had the finisher going first. Carries and trunk work are a stated Magnus principle; a third set of dumbbell press is not. | `01-METHODOLOGY.md §1.3` |
| 2026-08-24 | Accessory rounds drop from 3 to 2 on the peak week | The heaviest week has the longest main lift, so something had to give. Cutting accessory volume as intensity climbs is also just correct block periodization. | `01-METHODOLOGY.md §5.1` |
| 2026-08-24 | Movement choice prefers loadable, standard movements | The generator was picking bodyweight split squats and band lat pulldowns for someone with a full gym. Candidates are now narrowed: regressions (assisted pull-ups, band versions of cable work) only appear when nothing standard fits, and loadable movements win when there is something to load. | `01-METHODOLOGY.md §4.5` |
| 2026-08-24 | Single athlete, no auth; PIN gate instead | One user, so per-row ownership is meaningless. The t4m_ tables have RLS on with zero policies (public keys can read nothing) and all access is server-side with the service role. A missing APP_PIN in production refuses to serve rather than exposing a public log. | `02-DATA-MODEL.md §2` |
| 2026-08-25 | Chunk 14: kept `dynamic = 'force-dynamic'` on every route rather than removing it | Removing it would let Next attempt static prerendering of `/plan`, `/history`, `/settings` etc. at build time, which requires reaching Supabase from the build environment. That is not reliably available (this sandbox's network allowlist blocks it outright), so the safer win was taken instead: every repo read is wrapped in `unstable_cache` with tags (`profile`/`program`/`sessions`/`logs`), and every mutation in `actions.ts` calls `revalidateTag` for what it touched. This caches the *data fetches* regardless of the route's dynamic/static status, which is most of the available win without the build-time risk. Revisit removing `force-dynamic` in a later chunk once it can be verified against a real deploy. | `06-REDESIGN-PLAN.md` chunk 14 |
| 2026-08-25 | Chunk 14: `db()` returns a memoised singleton Supabase client | It was building a new client (URL parse, header setup) on every single call with no per-request state to justify it — `persistSession: false`, no cookies threaded through. One client for the life of the server instance. | `06-REDESIGN-PLAN.md` chunk 14 |
| 2026-08-25 | Chunk 15: `/plan` renamed `/today`, `/program` is a new route (not `/plan` renamed) | The redesign plan calls for `/today` (the hero/"what do I do right now" view) and `/program` (the whole block + eventually the routine list) as two distinct destinations. `/plan`'s old content — hero card, week strip, this-week's session list — became `/today`; `/program` is new, built for this chunk, showing every week of the active block plus the entry point into the builder stub. The plan's own redirect table still sends the literal old `/plan` URL to `/program`, not `/today`, exactly as written — kept as specified rather than second-guessed. | `06-REDESIGN-PLAN.md` §4, chunk 15 |
| 2026-08-25 | No packaged M3 `NavigationRail` — built from `Box`/`Stack` primitives | MUI (the version pinned in this repo) ships `BottomNavigation` but no rail component. `src/components/nav/NavRail.tsx` hand-builds one: icon-over-label, a pill active indicator, fixed at 88px. | `06-REDESIGN-PLAN.md` chunk 15 |
| 2026-08-25 | Chunk 16: generator-pool baseline is 101, not the plan's approximate "93" | The plan estimated the pre-expansion library at ~93 movements while scoping the work; the actual count in the shipped code was 101 (`grep -c 'mk('` on the original `exercises.ts`). The tripwire test pins the real number, not the plan's estimate — the estimate was for scoping only. | `06-REDESIGN-PLAN.md` chunk 16 |
| 2026-08-25 | Chunk 16: library organised by muscle group, not movement pattern | The 101 pre-existing movements were organised by `MovementPattern` (squat/hinge/lunge/…) in one file. The plan calls for one file per muscle group; keeping the old organisation would have meant two competing taxonomies in the codebase. All 101 were re-filed (not just copy-pasted — each also gained `primaryMuscles`/`secondaryMuscles`/`mechanic`/`force`) into the twelve muscle-group files. File placement is cosmetic; actual browse-group membership comes from `primaryMuscles` via `browseGroupsFor`, so a squat living in `quads.ts` and a squat living in `squat.ts` behave identically to the browser. | `06-REDESIGN-PLAN.md` chunk 16 §6 |
| 2026-08-25 | Chunk 16: `howTo` written for only a handful of movements | The field is explicitly optional in the plan. Writing 2–5 real, non-generic steps for all ~286 movements was not a good use of effort against everything else in the plan still to build; the `cue` field (required, present on every movement) already carries the one-line coaching point the session player and the exercise picker actually need. Chunk 17's detail page can lean on `cue` and backfill `howTo` opportunistically. | `06-REDESIGN-PLAN.md` chunk 16 |
| 2026-08-25 | Chunk 16: 286 movements shipped, not exactly "~300" | The plan's per-group minimums (chosen to guarantee every group feels populated) sum to 314; 101 of those were already covered by the pre-existing library, leaving 183 genuinely new movements to reach the minimums, plus 2 extra skill-gated demonstration entries (pistol squat, handstand push-up) — 286 total. Chunk 16 §7's own instruction is "a short, honest list beats padding"; the count was not inflated to round up to a nicer-looking number. | `06-REDESIGN-PLAN.md` chunk 16 §4, §7 |
| 2026-08-25 | Chunk 17: detail page ships without a chart or a full "your numbers" panel | Those depend on infrastructure that doesn't exist yet — hand-rolled SVG chart primitives (chunk 20) and the batched `exerciseContext` server function (chunk 19). Shipping a page that references either now would mean either faking data or leaving a visible dead section. The detail page instead ships a real, honest "Best set logged" line computed directly from `historyForExercise`, plus the full history list — genuinely useful today, and chunk 19/20 slot in without restructuring the page. | `06-REDESIGN-PLAN.md` chunk 17 |
| 2026-08-25 | Chunk 17: custom exercises deferred to chunk 18 | The plan places "+ Add exercise" in chunk 17 §3, but it needs a `t4m_custom_exercise` migration and a `getExercise()` fallback path that chunk 18's routine builder needs to design around anyway (custom exercises exist specifically so a self-built program can reference a movement the library doesn't have). Building it once, integrated with the builder's exercise picker, beats building it twice. | `06-REDESIGN-PLAN.md` chunk 17 §3, chunk 18 |
| 2026-08-25 | Chunk 18: no drag-and-drop dependency — up/down arrows only | The plan flagged this as a call to make based on bundle cost ("check the bundle cost against chunk 21's budget before adding a dependency... an up/down arrow fallback must exist for accessibility regardless"). Shipped the fallback as the *only* mechanism: reordering is a block-level up/down arrow, not per-item drag. Simpler, fully keyboard-accessible by construction, and adds nothing to the bundle. Revisit only if real usage shows the arrow-only flow is actually painful for long days. | `06-REDESIGN-PLAN.md` chunk 18 §4 |
| 2026-08-25 | Chunk 18: week-identical prescriptions, no `progressRoutine.ts` | The plan explicitly allowed this fallback ("If it is not clean, ship week-identical repetition and put progression in the backlog"). A linear or wave progression scheme needs its own design decisions (how much, how often, deload handling) that go beyond this chunk's scope — backlogged rather than rushed. | `06-REDESIGN-PLAN.md` chunk 18 §3 |
| 2026-08-25 | Chunk 18: single flat routine-editor page, not three separate full-screen steps | The plan sketched "3.1 Routine setup, 4.2 Day editor, 4.3 Item editor" as three screens. Shipped as: a create dialog (3.1, condensed — name/weeks/days only, matching what `createRoutine` actually needs up front), one editor page with day tabs holding the block-card editor (4.2), and an item-editor dialog (4.3) — same three concerns, fewer full-page transitions to build and to use for what is, in practice, a small amount of data per day. | `06-REDESIGN-PLAN.md` chunk 18 §4 |
| 2026-08-25 | Chunk 18: `Archetype = SessionArchetype \| 'CUSTOM'`, not `'CUSTOM'` folded into `ARCHETYPES` | Adding `'CUSTOM'` directly to the shared `ARCHETYPES` union broke every `Record<SessionArchetype, …>` exhaustiveness check in `assembleSession.ts` (PRIMER_FOR and friends), which must stay exhaustive over exactly the archetypes the generator's own skeletons produce. A separate `Archetype` type widens only `PlannedSession.archetype`, so the generator's lookup tables never need to account for a value they can never receive. | `src/core/types.ts` |
| 2026-08-25 | Chunk 19: `expected` defaults to 75% of training max when the caller doesn't specify a percent | The plan's example line ("Expected: 82% of 145 kg") shows one specific wave percentage; `exerciseContext` is called from four different places (picker rows, item editor, detail page, session player), most of which have no specific percent in mind — a picker row is asking "roughly what would I lift here", not "what does week 3 say". 75% sits in the generator's own working-week range (WAVE_4 weeks 1–2 run 70–75%) and is a reasonable, honest default; the builder's item editor overrides it with the routine item's own chosen percent so it reflects what that specific set is actually configured for. | `06-REDESIGN-PLAN.md` chunk 19 |
| 2026-08-25 | Chunk 19: `exerciseContext`'s DB round trip has no unit test | Consistent with the rest of `src/server` — `db.test.ts` tests connection/config logic, not live Supabase queries; no repo.ts function has a DB-backed test either, and this project has no mocked-Supabase test harness to add one for. The acceptance criterion that actually matters — `expected.weightKg` agreeing with `prescriptionFor`'s own output for the same inputs — is tested directly as a pure-formula cross-check (`exerciseContext.test.ts`), which doesn't need the DB at all. | `06-REDESIGN-PLAN.md` chunk 19 |
| 2026-08-25 | Chunk 20: no Body tab, no `/history/[sessionId]` diff view, no seed script | The plan's own scope was five tabs, a session-diff view, and a dev-only seed script — genuinely more than one chunk's worth once Strength/Volume/Consistency/Records were built properly. Body needs a new table for a single, low-traffic metric (bodyweight over time); the session diff is valuable but self-contained and doesn't block anything else; the seed script exists to make empty-state design easier to review, which this pass already handled by hand-designing every chart's 0/1/2-point states directly. All three are clean, well-scoped follow-ups, not corners cut on what shipped. | `06-REDESIGN-PLAN.md` chunk 20 |
| 2026-08-25 | Chunk 20: no `StackedBarChart` — muscle-group volume is a horizontal bar ranking, not a stacked-by-week chart | The plan sketched "sets by muscle group, stacked bar per week." With up to 12 muscle groups, a 12-colour stacked bar is hard to read and this app's palette only has three strong categorical accents (primary/secondary/tertiary) by design (`docs/04-DESIGN-SYSTEM.md` — calm, not a rainbow). A horizontal ranking (one bar per group, one colour, sorted by volume) answers the same real question — "what am I training most/least" — more legibly, at the cost of not showing the week-by-week trend within each group, which `weeklyVolume`'s own chart already covers in aggregate. | `06-REDESIGN-PLAN.md` chunk 20 §1 |
| 2026-08-25 | Chunk 20: the heatmap covers 12 weeks, not 12 months | A year of daily cells needs horizontal scroll at phone width or unreadably small cells; 12 weeks (84 days, one column per week) fits in a `PageContainer`-narrow column with 12px cells and reads at a glance. Extending the window is a parameter change, not a redesign, if it turns out to matter later. | `06-REDESIGN-PLAN.md` chunk 20 §3 |
| 2026-08-25 | Chunk 20: volume counts every logged set, ramps included | `t4m_logged_set` has no `kind` column — a ramp set (prescribed but never counted as working volume by the generator) is indistinguishable from a working set without joining back to the session's `blocks` JSONB by slot and set number. Counting every non-skipped set is a small, honest, and — critically — *consistent* simplification: it runs the absolute numbers slightly high but doesn't distort week-to-week trends, which is what the Volume tab is actually for. Documented in the code, not silent. | `src/server/analytics.ts` |
| 2026-08-25 | Chunk 21: rest timer's visible countdown stays `aria-live="off"`; a separate hidden region announces once, at zero | The plan asked for `aria-live` on the rest timer, "currently only on the set counter." A live-announcing countdown would re-read itself roughly every second and drown out anything else a screen reader user is doing during rest — worse than the silence it would fix. The visible timer stays `off`; a visually-hidden `aria-live="polite"` region (same pattern as `SessionPlayer.tsx`'s set counter) holds empty text until `remaining === 0`, then announces "Rest is up. Next set." exactly once. | `src/components/session/RestTimer.tsx` |
| 2026-08-25 | Chunk 21: `prefers-reduced-motion` handled by one global `GlobalStyles` media query, not a flag threaded per component | Every animated thing in this app (MUI's `Collapse`, `Fade`, `Grow`, hover transitions) already animates via plain CSS transitions/animations. A single `* { transition-duration: 0.01ms !important; ... }` rule under the media query, mounted once in `Providers.tsx`, covers all of them with no per-component wiring and nothing to forget to add to a new component later. | `src/theme/Providers.tsx` |
| 2026-08-25 | Chunk 21: the service worker (`public/sw.js`) precaches the static shell and an offline fallback page only — it does not cache or serve stale dynamic pages | This app is fundamentally online and data-driven; every real route reads current program/session/log state from Supabase. Serving a cached `/today` or `/session/[id]` while offline would show stale or wrong training state with no visual indication it's stale — actively misleading, not helpful. The SW's job is narrower and safer: replace the browser's own connection-error page with the app's own `/offline` screen on a failed navigation, and keep the tiny static shell (icon, manifest) available without a round trip. Sets logged offline remain the outbox's job (`src/components/session/outbox.ts`), unrelated to this SW and unaffected by whether it's even registered. | `public/sw.js`, `src/app/offline/page.tsx` |
| 2026-08-25 | Chunk 21: `/offline` and `/sw.js` excluded from the PIN-gate middleware matcher | Both must be reachable with literally zero network round trips available (that's the point of `/offline`) or, for `sw.js`, must return real `application/javascript`, never an HTML redirect — a browser fetching a service-worker script rejects anything but a genuine JS response and registration fails outright rather than degrading gracefully. Same reasoning the plan already applied to excluding `/unlock` itself from the gate. | `src/middleware.ts` |
| 2026-08-25 | Chunk 21: manifest ships two SVG icons (`any` + `maskable`), no PNG sizes, no `screenshots` | No image-generation tooling and no way to capture real, populated-app screenshots in this sandbox (the same live-Supabase network restriction that has blocked every chunk's data-path verification) — fabricating placeholder screenshots would be worse than shipping none. SVG icons at `sizes: 'any'` are honoured by every modern installer; PNG fallbacks are a clean follow-up if a specific platform is ever found not to render the SVG. | `src/app/manifest.ts` |
| 2026-08-25 | Chunk 21: builder row touch targets widened to 48×48 via `sx` overrides on `IconButton size="small"`, not switched to the default `size="medium"` | The default 48px `IconButton` would visually bloat the routine editor's already-dense block rows (four icon buttons across one row: split, remove, move up, move down). Keeping the small 20px icon glyph but forcing the button's own box to 48×48 hits the same WCAG/Material touch-target minimum without changing how the row reads. The row's own primary tap target (exercise name → edit) was also promoted from a bare `onClick`-on-`Box` — invisible to a screen reader and unreachable by keyboard — to a real `ButtonBase`, matching the fix already made to `SetRow.tsx` in this same chunk. | `src/components/builder/RoutineEditor.tsx` |
| 2026-08-25 | Chunk 21: `docs/02-DATA-MODEL.md` rewritten around the live `t4m_*` schema; the original multi-user design kept verbatim as an appendix | The document had described a schema — `auth.uid()`-scoped RLS, normalized `session_blocks`/`block_exercises`/`prescribed_sets` tables — that was never built; the actual schema (one permissive `for all to anon, authenticated using (true)` policy per table, a single `blocks` jsonb column, no `user_id` anywhere) is a deliberate single-athlete simplification made back at the 2026-08-24 "single athlete, no auth" decision above, just never reflected in this doc. Live schema pulled directly from Supabase (`list_tables`, plus `pg_policies`/`pg_indexes` for the parts `list_tables` doesn't surface) rather than reconstructed from memory. The old design is preserved, not deleted, as the starting point if this app ever needs a second athlete. | `02-DATA-MODEL.md` |
| 2026-08-25 | Chunk 21: performance budget table records every named route over its own chunk-21-specified target — by 3 kB on `/program/builder` up to 62 kB on `/session/[id]` — not adjusted to show green | Chunk 21 §4 named four explicit targets (`/today` ≤130 kB, `/exercises` ≤160 kB, `/program/builder` ≤190 kB, `/session/[id]` ≤170 kB) and said plainly: a blown budget "is a finding to report, not a number to edit." The shared floor every route pays before its own code runs — MUI's emotion runtime plus the App Router client runtime — is 102 kB as of `next build` at the end of chunk 21, already consuming most of `/today`'s entire budget on its own; it rose once, at chunk 15 (M3 palette module augmentation + the always-mounted nav shell), and has held flat since, never reconciled against the targets set before that chunk. Recorded with the real numbers, the real gaps, and the two largest misses named as next-chunk candidates (`/exercises`'s filter chips, `/session/[id]`'s eager rest-timer/readiness-dialog code), rather than silently redefined to match reality. | `06-REDESIGN-PLAN.md` §9 |
| 2026-08-26 | #10 fix: `hardSets` reseeds to 1 (not the true prior count) whenever `session.autoregulated` is already true | There's no dedicated counter column, only the existing boolean, and adding one means a migration this environment has no live database to run or verify. The backoff factor only ever branches on "has this happened before" (`>= 2` vs `< 2` hard sets), never the exact count — so seeding to 1 on any prior `true` reproduces the correct next factor in every case (one prior backoff or several), not an approximation of one. | `src/components/session/SessionPlayer.tsx`, `07-PRODUCTION-REVIEW.md` #10 |
| 2026-08-26 | Client-error reporting (#26 follow-up) is a plain route handler (`/api/log-client-error`), not a server action | `error.tsx` and `global-error.tsx` both wrap every route, `/unlock` included (`global-error.tsx` replaces the root layout on a crash). A `'use server'` export imported there would list `app/unlock/page` as one of its callable workers in the build manifest — exactly the shape `scripts/check-action-isolation.mjs` (the #1 regression guard) exists to fail the build over. A route handler carries no such risk: it's plain HTTP, invisible to that manifest. | `src/app/api/log-client-error/route.ts`, `07-PRODUCTION-REVIEW.md` #1, #26 |
| 2026-08-26 | #22 follow-up: only `ReadinessDialog`/`RestTimer` were split out of `/session/[id]`; the exercise-library-in-client-bundle cause was left alone | Splitting the two conditionally-rendered dialogs was measured (235 kB → 228 kB First Load JS) and low-risk. The actual weight — the ~123 kB exercise library imported directly into `ExerciseBrowser`, `ExercisePickerDialog`, and everything calling `getExercise` — needs the data-layer restructuring #22 already named (a slim server-side index, detail fetched on demand), which touches enough files across enough routes that pushing it untested straight to production in the same pass as everything else here was a worse trade than reporting the real number and leaving it for its own pass. | `docs/07-PRODUCTION-REVIEW.md` #22 |
| 2026-08-26 | A dev-only seed script, a `t4m_custom_exercise` UI, and CI actually gating Vercel deploys were assessed and left undone | A seed script means guessing at exact insert shapes for every `t4m_` table with no live database in this environment to catch a mistake — unverified database-writing code is a worse trade than none. Custom exercises need a DB-backed exercise threaded into every place the static library is read (browser, both pickers, builder, session player, analytics) without breaking `src/core`'s pure-no-database boundary — a real feature, not a fix. CI-gated deploys need either a paid Vercel feature or a homemade `ignoreCommand` that races the same push's two triggers (GitHub Actions and the Vercel build both start on push) — not worth the risk of silently skipping good deploys in the one place a mistake means production stops updating. | `docs/07-PRODUCTION-REVIEW.md` "Status as of 2026-08-26" |
| 2026-08-26 | #2's RLS-tightening migration also picked up `t4m_rate_limit`, not just the 13 tables named when the migration was drafted | The migration discovers tables by `like 't4m/_%' escape '/'` at execution time rather than a fixed list, specifically so it covers whatever `t4m_` tables exist when it runs. `t4m_rate_limit` matches that pattern too, even though `08-RLS-TIGHTENING.md` originally expected it to be skipped (it already had no client-facing policy at all). Confirmed harmless after the fact via `pg_policies`: `service_role` bypasses RLS regardless of what policies exist, so an extra redundant `t4m_rate_limit_service` policy changes nothing. Doc corrected rather than left wrong now that this was actually run and checked. | `docs/08-RLS-TIGHTENING.md` |
| 2026-08-29 | The weight on a set is typed by the athlete; nothing pre-fills it from the plan or from history | A logged set is a record of what was lifted, and neither the prescription nor last week's log knows that — pre-filling one and letting a single ✓ commit it manufactured training data that reads exactly like the real thing. The prescription and the last-time number are still shown: as the field's placeholder, as a one-tap "Use 100 kg", and as the value the +/− steppers adopt on their first tap from empty. The one thing that does pre-fill is the weight the athlete already chose for that movement earlier in the same session (below). | `README.md` "During a session" |
| 2026-08-29 | The first weight entered for a movement carries over to every later set of it in that session | With nothing pre-filled, typing the same number four times for four sets would be the price of the decision above; carrying it over means one entry per movement, and the number carried is the athlete's own. `0` carries too — it is the explicit "bodyweight, no load" answer, which is why the carried value is `number \| null` and never a truthiness test. The RPE ≥ 9.5 backoff scales what is carried as well as the prescription, or the first tap on the next set would quietly put the full load back on the bar. | `README.md` "During a session" |
| 2026-08-29 | A program materialised from a routine can be edited while it is being trained; the edit rewrites only what has not been trained yet | Re-scheduling was the only way to change a program you were in the middle of, and it abandoned the block and restarted week 1 today — so a change of mind about Thursday's accessory work cost the whole block's progress. `updateProgramFromRoutine` re-materialises over the live program instead: sessions that are finished, in progress, skipped, or holding logged sets are left untouched, everything still merely planned is rewritten on the dates it was already scheduled for, and the program row, its start date and its history all survive. Restarting from today is still available, behind a confirmation. A generated block has no routine behind it, so it is duplicated into one first and then *adopted* — the same in-place rewrite, behind its own confirmation because it is a bigger change: from that point the block is the routine's, and the generator's wave over the weeks ahead (peak week, deload) goes with it. | `README.md` "Building it yourself", `02-DATA-MODEL.md` (no schema change) |
| 2026-08-30 | Chunk 22: `flashRow`/`drawCheck` keyframes are declared globally in `theme.ts`, referenced by plain string name | `@emotion/react`'s `keyframes()` helper only registers its `@keyframes` rule when its result is threaded through emotion's own `css`/`styled`/`sx` serializer. `SetRow`'s self-drawing check uses a real inline `style` object on an SVG `<path>` (not `sx`) — interpolating a `Keyframes` object into that via a template literal silently produces an unregistered animation name that never animates. Declared once in `MuiCssBaseline.styleOverrides` instead, next to `.tnum`, and referenced as `'flashRow 400ms ease'` / `'drawCheck 250ms ease forwards'` — no per-component generation, no serializer to route through correctly. | `src/theme/theme.ts`, `src/components/session/SetRow.tsx` |
| 2026-08-30 | Chunk 22: focus mode's cursor advances on the not-done → done *transition*, not on "the cursor's current movement happens to be done" | The first implementation was a `useEffect` re-checking `movementDone` on every render where `logged`/`cursor` changed. It broke the brief's own acceptance case: going back to fix an already-logged set landed on a movement that was (still) fully done, so submitting the correction immediately re-advanced the cursor forward again — the athlete never got to see the fix land. Fixed with a `pendingAdvance` ref set only inside `complete()`, and only when a movement crosses from not-done to done, consumed exactly once by an effect and left alone by any render where the cursor merely sits on an already-done movement (manual back-navigation included). | `src/components/session/SessionPlayer.tsx`, `chunk-22-player-feel.md` §2 |
| 2026-08-30 | Chunk 23: `t4m_program` gained a `tm_changes jsonb` column, applied directly via a live Supabase migration | Every prior chunk's `DECISIONS.md`/`PROGRESS.md` entries note "no live database in this environment" — that changed this session; `mcp__Supabase__*` tools reach the real project (`evlxbewvsgrlncvtagmf`, confirmed by `t4m_` tables present alongside the unrelated apps sharing it). `rollOverTrainingMaxes()`'s `reason` string (e.g. "all reps at RPE 8, full jump") is computed once and never otherwise persisted — `t4m_training_max` is an append-only history table, so `from`/`to` could in principle be reconstructed after the fact, but `reason` could not. The retrospective's acceptance criterion ("TM deltas with their reasons") needed it stored verbatim, not recomputed. Additive, nullable, backward-compatible — applied and verified live (`information_schema.columns`), not merely written and hoped. | `docs/02-DATA-MODEL.md`, `src/server/repo.ts`, `chunk-23-reward-loop.md` §1 |
| 2026-08-30 | Chunk 23: the retrospective is reachable from `/today` *before* `startNextBlock` ever runs, and its training-max section says "not decided yet" until it has | The brief's finding #4 implies the block's own data (tonnage, adherence, PRs, peak-week top sets) is already sufficient without waiting for the roll-over — and `NextBlockCard`'s "See how it went" link is offered alongside "Start next block", not gated behind it. But `tm_changes` genuinely does not exist before `startNextBlock` computes it — recomputing a preview via the same peak-week logic risks disagreeing with the real decision once it is made (and, after the click, would double-apply against the already-rolled-over training max). Read, never recomputed: `null` renders as "not decided yet", populated once `startNextBlock` has actually written it. `startNextBlock`'s own success path now also navigates straight to the retrospective, since that is the one moment the reasons are freshly computed. | `src/components/today/NextBlockCard.tsx`, `src/app/program/complete/page.tsx` |
| 2026-08-30 | Chunk 23: `LineChart` takes a required `chartId` prop instead of the brief's suggested `useId()` | `useId()` needs a Client Component render (a Fiber, not RSC serialization) to call at all. `LineChart` has never carried `'use client'`, and `BodyTab.tsx` renders it as a genuine Server Component with zero client JS — calling `useId()` there would fail outright. A caller-supplied id (the exercise id, `'bodyweight'`) is simpler, stays server-renderable everywhere, and is exactly as unique as the caller makes it — enforced as a required prop, not an optional one with a collision-prone default. | `src/components/charts/LineChart.tsx` |
| 2026-08-30 | Chunk 23: `BarChart`/`HorizontalBarChart` were left as they were — the brief's "BarChart gets 3.1 and 3.2 only" was not applied | Read most plausibly as "area fill" and "x-axis labels" (§3's own sub-items 1 and 2). Area fill under a path has no bar-chart analogue — bars are already filled shapes, not an outline with space beneath it. X-axis labels already exist under every bar in the current `BarChart` (small week counts, not a crowded 12-point series) — the "first/middle/last only" fix in `LineChart` exists specifically because a dense series makes every label unreadable, a problem `BarChart`'s typical week-count series does not have. Recorded rather than forcing a change that duplicates existing behaviour or doesn't apply. | `src/components/charts/BarChart.tsx` |
| 2026-08-30 | Chunk 23: `BodyMap`'s peak/whole-figure groups (`cardio`, `mobility`, `full_body`, and whichever of `chest`/`core`/`quads`/`back`/`hamstrings_glutes` isn't visible from a given side) render as a small badge dot in that silhouette's margin, not a body region | Not every `MuscleGroup` has a natural single anatomical region visible from *both* front and back — the chest doesn't show from behind, hamstrings don't show from the front, and cardio/mobility/full-body were never regions to begin with (`GROUP_MUSCLES` maps `mobility`/`full_body` to `[]`). A badge keeps `Record<MuscleGroup, string>` genuinely exhaustive on both silhouettes (a group added to `muscles.ts` without an entry in either is still a typecheck failure) without pretending a region is visible where it structurally is not. | `src/components/charts/BodyMap.tsx` |
| 2026-08-30 | Chunk 24 §1: the rest-timer notification was **not** tested on a real phone — implemented as best-effort per the brief's own architecture, its actual backgrounded behaviour is unverified, not "confirmed working" | This environment has no physical device to background a session player on and observe. The brief's instruction was explicit: "test it for real… and write what you observed" — a documented limitation counts as done, an untested claim does not, so no observed-behaviour claim is made either way. What *is* true by construction, not observation: `RestTimer.tsx`'s foreground path (vibrate + WebAudio beep) is unaffected and unchanged; the new part is a `postMessage({type:'rest-timer', endsAt, body})` to the active service worker, which schedules its own `setTimeout` and calls `showNotification` if it is still alive when the timer elapses (`public/sw.js`). Both the component and the worker gate on `Notification.permission === 'granted'` first and no-op otherwise. Whether a real browser keeps that worker alive across a 90-second rest with the tab backgrounded is exactly the open question the brief flagged — most engines terminate an idle worker well before that, per general platform documentation, but this was not verified against an actual device in this session. Recorded as unverified rather than asserted; the user is best placed to background a real session and report what they see, at which point this entry should be updated with the real answer. | `src/components/session/RestTimer.tsx`, `public/sw.js` |
| 2026-09-02 | Chunk 25: `@anthropic-ai/sdk` added, pinned exact at `0.123.0` | Latest stable on npm at the time this chunk was written (`npm view @anthropic-ai/sdk dist-tags` → `latest: 0.123.0`), matching every other dependency's exact-pin convention (`00-CONTEXT.md §5`). Not on `00-CONTEXT.md §3`'s disallowed list — a state library, ORM, CSS framework, second component kit, or second charting library — recorded anyway since every dependency choice in this repo's history is. | `package.json` |
| 2026-09-02 | Chunk 25: `t4m_coach_message`/`t4m_coach_usage` shipped with a `service_role`-only RLS policy each, not the `anon, authenticated` pattern `11-COACH-PLATFORM.md §3` describes | §3 was written before `docs/08-RLS-TIGHTENING.md` landed (2026-08-26) and never updated — every other live `t4m_` table has carried a single `service_role`-only policy since then, confirmed directly via `pg_policies` before writing this migration. The chunk brief's own instruction ("copy the pattern from a recent migration rather than writing it from scratch") points at the *live* convention, which is stronger authority here than an earlier section of a planning doc that predates the change it's describing. Both new tables verified live afterwards the same way chunk 23 did: `information_schema.columns`/`information_schema.tables` for the schema, `pg_policies` for RLS — all 16 `t4m_` tables now (14 before this chunk, per `08-RLS-TIGHTENING.md`) carry exactly one `service_role`-only policy, no `anon`/`authenticated` grant anywhere. | `docs/11-COACH-PLATFORM.md §3` (now stale on this one point, left as historical record rather than rewritten) |
| 2026-09-02 | Chunk 25: model ids and per-token prices came from the `claude-api` skill's cached table (2026-06-24), not `11-COACH-PLATFORM.md §2`'s own numbers | The plan document says explicitly not to trust its own figures blindly and to check the skill instead. Landed: chat/debrief → `claude-haiku-4-5` ($1.00/$5.00 per MTok), proposal (chunk 28, named here so `anthropic.ts`'s model map already has the right id waiting) → `claude-sonnet-5` ($2.00/$10.00 per MTok) — both bare ids, no date suffix, matching the skill's explicit "never append date suffixes" instruction; `11-COACH-PLATFORM.md §2` had written `claude-haiku-4-5-20251001`, which is stale. | `docs/11-COACH-PLATFORM.md §2`, `src/server/coach/anthropic.ts` |
| 2026-09-02 | Chunk 25: `capCheck`'s boundary — spending *exactly* the cap still allows the next call; only spending *strictly over* it refuses | The chunk brief asked for this to be picked and documented, not derived from anything else in the plan. Reads most naturally against "up to $2/day" phrasing, and keeps the two round-number defaults ($2/$20) from refusing a call that lands exactly on them by construction. | `src/core/coach/costCap.ts` |
| 2026-09-02 | Chunk 25: `listCoachMessages`'s trim point (`docs/11-COACH-PLATFORM.md §7`'s "chunk 25 picks N") is 20 messages | ~10 user/assistant turns — comfortably inside Haiku's 200K context for this app's short, factual replies, while keeping the assembled prompt (and its cost, since every history token is billed again on every turn with no caching in this chunk) bounded regardless of how long the chat log has been running. | `src/server/coach/repo.ts` (`COACH_HISTORY_LIMIT`) |
| 2026-09-02 | Chunk 25: `coachCompletion` records real usage even when the response's `stop_reason` is `"refusal"` | Real input/output tokens were still spent making the call regardless of whether the model answered — `docs/11-COACH-PLATFORM.md §2`'s "followed by an insert recording what it actually cost" doesn't carve out an exception for a declined answer, and skipping the insert would under-report real spend against the cap it exists to protect. | `src/server/coach/anthropic.ts` |
| 2026-09-02 | Chunk 26: a test week's target is a top single (`reps: 1`, RPE 9) at the current training max, with the note explicitly inviting a rep-max or an extra on-the-fly set instead | The chunk allowed either shape "your call." A single mirrors the peak week's own top-set format exactly (no new UI needed to express it), and the app's chunk-24 on-the-fly-sets feature already lets the athlete go heavier live if it moves well — so prescribing a conservative, TM-anchored single and leaving room to push further beats guessing at a maximal number up front. | `src/core/progression/testWeek.ts` |
| 2026-09-02 | Chunk 26: `buildTestWeek` takes an args object (`program`, `testExerciseIds`, `trainingMaxes`, `startDate`, `increment`, `paceFactor`) rather than the chunk file's literal two-argument `buildTestWeek(program, testExerciseIds)` | Purity forbids `Date.now()` inside `src/core`, so a start date has to come in as a parameter — same reasoning `materializeRoutine`'s own `MaterializeArgs.startDate` already established. Training maxes also can't safely come from `program.input.trainingMaxes`: a routine-builder-sourced block's `input` is `{ routineId, source: 'custom' }` (see `scheduleRoutine`) and carries no training maxes, `microPlates`, or `paceFactor` at all — the server has to supply live values regardless of what produced the finished block. | `src/core/progression/testWeek.ts` |
| 2026-09-02 | Chunk 26: `trainingMaxFromTestResult`'s `reps > 1` branch runs `trainingMaxFromOneRepMax(epley(w, r))` — the plain 90% ratio — not `estimateTrainingMax`'s extra 5% first-block haircut | `estimateTrainingMax`'s conservatism exists because onboarding has zero trust in a self-reported number; a mid-block, logged, supervised test earns the plain ratio instead. The two formulas deliberately disagree — a unit test pins the difference so nobody "fixes" this into agreement later. | `src/core/progression/testWeek.ts` |
| 2026-09-02 | Chunk 26: a started test week replaces the finished block as `t4m_program.status = 'active'`, marked by `input.source = 'test_week'` (plus `testExerciseIds`/`parentProgramId`) — the same "not a real `GeneratorInput`" trick chunk 18's `scheduleRoutine` already plays for a builder-sourced block | This is what makes "no new player code" literally true: `/today`, the session player, and `logSets` all key off "the active program's sessions," so a test week has to *be* the active program, not a second concurrent one, for them to render and log it with zero changes. The finished block's own id (`parentProgramId`) is where the eventual tested/inferred training-max changes get written, so `/program/complete?programId=<that id>` still resolves correctly once the test week is applied. | `src/server/testWeek.ts` |
| 2026-09-02 | Chunk 26: `rollOverTrainingMaxes(program, testedOverrides?)` now takes the block to roll over as a parameter instead of calling `getActiveProgram()` itself, and a new `finishBlock` helper in `actions.ts` is the one shared tail both `startNextBlock` and `applyTestWeekResults` call | By the time `applyTestWeekResults` runs, the *active* program is the test week itself, not the block being rolled over — the parent has to be looked up by id (`testWeekMeta(active).parentProgramId`) and passed in explicitly. This is exactly the "one shared code path with an optional pre-seeded max map" the chunk brief asked for, rather than a second implementation of "start the next block." `startNextBlock`'s own behaviour is unchanged: it still captures `getActiveProgram()` itself and passes it straight through, with an empty override list by default. | `src/server/nextBlock.ts`, `src/server/actions.ts` |
| 2026-09-02 | Chunk 26: the test-week/inferred split is written as two separate `setTrainingMaxes` calls (one per `source`) inside `rollOverTrainingMaxes`, rather than widening `setTrainingMaxes`'s own signature to take a source per row | `setTrainingMaxes` already exists and is used elsewhere with a single source for the whole batch; a per-row source would ripple that signature change into every other caller for a need only this one function has. Splitting the values into a `progressedValues`/`testedValues` pair and calling it twice (both writes share one `effectiveFrom`) keeps the change local. | `src/server/nextBlock.ts` |
| 2026-09-02 | Chunk 26: "Test your maxes first" lives in a new shared client component (`BlockDecisionButtons`), used both by `NextBlockCard` (`/today`) and `/program/complete`'s pre-decision state | The chunk brief's prose ("same card, not a separate page") and its acceptance box ("`/program/complete` offers 'Test your maxes first'") name two different places the decision already reachable from — `/today`'s `NextBlockCard` is where "Start next block" already lived, and "See how it went" already links to `/program/complete` *before* a decision is made. Satisfying both without duplicating the two server calls (and their pending/error state) meant factoring the buttons out once rather than picking one home and leaving the other's acceptance box unmet. | `src/components/today/BlockDecisionButtons.tsx` |
| 2026-09-02 | Chunk 26: `computeTestedOverrides` takes the *heaviest logged, non-skipped* set for a tested exercise (ties broken by reps) rather than "whatever landed in the prescribed top-set slot" | The prescribed single is a starting target, not a ceiling — chunk 24 already lets the athlete log extra on-the-fly sets, and the whole point of a test week is to capture what was actually lifted. Reading back the best real attempt, not the slot the generator happened to prescribe, is what makes "exactly equal to what was logged" true regardless of how the session actually went. | `src/server/testWeek.ts` |
| 2026-09-02 | Chunk 27: `src/core/coach/debrief.ts`'s `buildDebriefContext` takes narrow local structural types (`DebriefSession`/`DebriefLoggedSet`/`DebriefPr`/`DebriefPreviousSession`), not the chunk file's literal `SessionRow`/`LoggedSetRow`/`Pr` | Same forced move chunk 25's `context.ts` already made and for the same reason: `src/core` may not depend on `@/server` at all (`00-CONTEXT.md §4`, `eslint.config.mjs`'s `no-restricted-imports`), and those three types live in `src/server/repo.ts`. `DebriefSession.blocks` is typed as the real `SessionBlock[]` from `@/core/types` rather than re-shaped, since that type is genuinely pure already — no need to narrow what doesn't need narrowing. | `src/core/coach/debrief.ts` |
| 2026-09-02 | Chunk 27: a debrief is generated for *any* completed session, including one with zero (or partially skipped) logged sets, rather than special-cased or skipped | The chunk brief left this an open call either way. `buildDebriefContext` already renders a coherent "no sets were logged" fact with no code branch needed, and a fully-skipped session is exactly the kind of thing a coach should have an honest reaction to (`docs/11-COACH-PLATFORM.md §0`'s "your squat max probably went up" is not a sentence this app should generate — the mirror case, staying silent about a skipped session, isn't a sentence this app should skip generating either). `generateSessionDebrief` does refuse outright — before calling the model — for a session whose `status` isn't `'completed'` (planned/in-progress/skipped-status), since nothing has actually happened yet to react to; that guard isn't in the chunk file's literal text either but follows directly from what a debrief is for. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 27: "vs last time" framing (`previousSessionsSamePattern`) is sourced from the existing `repo.recentSessions(40)` (completed/skipped, cross-block, already cached under `TAGS.sessions`), filtered in `actions.ts` by `mainPattern` and an earlier `scheduledDate`, rather than a new dedicated query | `recentSessions` already exists for exactly this "recent history" shape (`/history`'s own pagination) and already returns full `SessionRow`s with `mainPattern`/`scheduledDate` — a second query would duplicate it for a narrower filter that's just as cheap to apply in JS on the same 40 rows. The 40-row cap means a same-pattern session further back than that won't be counted; consistent with `/history`'s own cursor-based "not the full history" behaviour, not a new limitation. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 27: `DebriefCard` is a plain (non-`next/dynamic`) client component, unlike `ReadinessDialog`/`RestTimer`/`ListView` on the same route | Measured, not assumed: `/session/[id]`'s First Load JS moved 235 → 236 kB (route-own JS 18.8 → 20.3 kB) with the card shipped eagerly — small enough that a second dynamic-import boundary (and its own loading fallback) would add more complexity than it would save, unlike the two heavier, conditionally-rendered pieces chunk 22/07-PRODUCTION-REVIEW.md #22 already split out. | `src/components/session/DebriefCard.tsx` |
| 2026-09-02 | Production incident (found ahead of chunk 29): `@anthropic-ai/sdk` re-pinned from `0.123.0` to `0.117.1` | Every deploy since chunk 25 landed had failed at `pnpm install` on Vercel: `[ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION]` — `0.123.0` was published 2026-09-01, inside Vercel's pnpm supply-chain "minimum release age" window (rejects a lockfile entry too soon after its own publish, a guard against a just-published/compromised package version). Invisible in this sandbox because nothing here runs that policy — lint/test/build were all green against the too-new pin. Re-pinned to `0.117.1` (published 2026-08-13, ~3 weeks old at the time of this fix, comfortably clear of the policy window) rather than the newest patch on npm; `pnpm-lock.yaml` regenerated via `pnpm install`, `pnpm build`/`pnpm test`/`pnpm lint`/`pnpm typecheck` all re-run clean against the new pin — nothing in `src/server/coach/anthropic.ts`'s usage (`Anthropic` constructor, `messages.create`, the `RateLimitError`/`AuthenticationError`/`APIError`/`APIConnectionError` classes) differs between these two versions. Shipped as its own standalone commit ahead of chunk 29's, so the production fix reaches `main` without waiting on the rest of that chunk. | `package.json`, `pnpm-lock.yaml` |
| 2026-09-02 | Chunk 29 §1: the coach rate limit reuses the existing `t4m_rate_limit` table with a constant `'coach'` bucket, rather than a new dedicated table; 10 messages/minute | `t4m_rate_limit`'s shape (a bare `(id, ip, created_at)` insert-and-count table) fits a message-burst window just as well as it fits `/unlock`'s 15-minute attempt window — only the RPC's own window/limit differ, not the table. A new `t4m_check_coach_rate_limit()` RPC always reads/writes the literal string `'coach'` into the existing `ip` column instead of deriving anything IP-like — there's no IP concept that means anything for a single-athlete server action the way it does for the public `/unlock` endpoint, so one athlete gets one fixed bucket. 10/minute chosen as "a generous ceiling for one athlete texting their own coach, tight enough to stop a scripted burst," per the chunk file's own suggested starting point — confirmed live: 10 calls allowed, the 11th refused. | `src/server/coach/rateLimit.ts`, migration `t4m_coach_rate_limit` |
| 2026-09-02 | Chunk 29 §1: `generateSessionDebrief` does not get a rate limit | Chunk 27's own caching (`coachRepo.getDebriefForSession` checked before ever calling the model) already limits it to at most one real `coachCompletion` call, ever, per session — every later view of the same session returns the cached row with zero model calls. A burst limit exists to stop repeated real calls in a short window; a call path that can already only ever fire once has nothing left for a second limit to guard against. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 29 §2: `MessageInputLazy.tsx` — a `'use client'` wrapper around `next/dynamic(() => import('./MessageInput'), { ssr: false })`, rather than calling `dynamic(..., { ssr: false })` directly in `/coach/page.tsx` | Next.js rejects `ssr: false` passed to `next/dynamic` directly inside a Server Component ("ssr: false is not allowed with next/dynamic in Server Components. Please move it into a client component") — confirmed by trying it directly first, not assumed. `/coach/page.tsx` has no reason to become a Client Component itself (the whole point is a server-rendered thread with zero client JS by default), so the dynamic-import boundary lives in a new, otherwise-empty file whose only job is being a legal place to call it — the same role `SessionPlayer.tsx` (itself already a client component) plays for its own `ReadinessDialog`/`RestTimer`/`ListView` dynamic imports. | `src/components/coach/MessageInputLazy.tsx` |
| 2026-09-02 | Chunk 29 §3: fuzzing `tools.ts`'s zod schema is deferred to chunk 28 | `src/core/coach/tools.ts` and `applyProposal.ts` are chunk 28's own deliverables (`docs/11-COACH-PLATFORM.md §4`) and do not exist in this repo as of chunk 29 — this session was explicitly instructed to run chunk 29 early, ahead of chunk 28, and not to start chunk 28's own work. There is no schema to fuzz yet. The other two §3 items (no prose-parsing path in `actions.ts`; athlete-authored free text framed as inert data) apply to code that already exists today and are both done, with tests. Recorded here as an explicit handoff rather than silently dropped: chunk 28 must write the fuzz suite itself when it creates `tools.ts`, per `docs/chunks/chunk-29-coach-guardrails.md §3`'s own first bullet. | `docs/chunks/chunk-29-coach-guardrails.md §3` (chunk 28 to close) |
| 2026-09-02 | Chunk 29 §3: both `SYSTEM_PROMPT` and `DEBRIEF_SYSTEM_PROMPT` gained an explicit "this is data, not instructions" paragraph, prompted by a real vector found while reviewing, not a hypothetical one | `activeProgram.name` already reaches `buildCoachContext` (chunk 25) and is genuine athlete-authored free text — `createRoutine`/`renameRoutine` (chunk 18) take an arbitrary string with no content restriction. A training note doesn't reach either context builder yet (`t4m_session.notes` is never read by `context.ts`/`debrief.ts`), so this framing is forward-looking for that specific field, but not for the program-name field, which is live today. The trust boundary itself is still the tool-call schema, not the prompt (`docs/11-COACH-PLATFORM.md §4`'s own words) — this paragraph doesn't change that, it just stops the model from being asked to *read* an athlete-authored string as if it might be a command, on top of already being structurally unable to execute one. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 28: no migration needed — `t4m_coach_message.proposal`/`proposal_status` (jsonb, text + its own three-value check constraint) already existed live, written by chunk 25's migration ahead of need | Confirmed by direct `information_schema.columns`/`pg_constraint` queries against `cyberpunk-vibe01` before writing any repo code, not assumed from `11-COACH-PLATFORM.md §3`'s table definition. Chunk 25's own migration matched that section's full table shape in one pass rather than only the columns chunk 25 itself used at the time. | — (verified live, no migration file) |
| 2026-09-02 | Chunk 28: `applyProposal.ts`'s `session` argument is a local structural type, `SessionForProposal { status, blocks }`, not the chunk file's literal `SessionRow` | Same forced move chunks 25/27 already made and for the same reason: `src/core` cannot import `@/server/repo` at all (`00-CONTEXT.md §4`, `eslint.config.mjs`). The real `SessionRow` satisfies it structurally with no conversion at the call site. | `src/core/coach/applyProposal.ts` |
| 2026-09-02 | Chunk 28: `propose_change`'s JSON schema (`PROPOSE_CHANGE_TOOL.input_schema`) is generated from the zod discriminated union itself via zod v4's own `z.toJSONSchema`, not hand-written and kept in sync by a test | The chunk file asked to check whether the pinned Anthropic SDK (`0.117.1`) can derive a tool schema from zod directly — it can't (its `.d.ts` files carry no such helper, checked directly) — or whether one has to be hand-written and synced by a round-trip test. Zod v4 (already a dependency, `^4.4.3`) ships `toJSONSchema` itself, which is the stronger of the two options the chunk file offered: there is no second schema to drift out of sync with the first, by construction. The generated `{ oneOf: [...] }` is wrapped under an explicit `type: 'object'` (a JSON Schema `type` and `oneOf` at the same level combine with AND semantics, and Anthropic's own `Tool.InputSchema` requires `type: 'object'` at the top). `tools.test.ts` still asserts the shape (three branches, `additionalProperties: false` each) as a regression guard, not as the sync mechanism. | `src/core/coach/tools.ts` |
| 2026-09-02 | Chunk 28: `adjust_sets.sets` is an absolute new set count, not a `+1`/`-1` delta | The chunk file said "pick one, record which." An absolute count is one unambiguous number both the model produces and `ProposalCard` shows as a plain "3 → 4" diff; a delta would need the model to already know the current count (which it does — it's listed in the prompt via `proposalTargets.ts` — but a delta still leaves more room for an off-by-one than "make it 4" does). | `src/core/coach/tools.ts` |
| 2026-09-02 | Chunk 28: `applyProposal.ts` throws seven distinct `DomainError` subclasses, one per validation rule (`SessionNotEditableError`, `BlockOrSlotNotFoundError`, `MainLiftProtectedError`, `UnknownExerciseError`, `ExerciseNotPermittedError`, `SetNotFoundError`, `LoadOutOfRangeError`), rather than one generic error carrying a `code`/`reason` field | Matches this file's own established pattern (`SessionOverBudgetError`/`BalanceUnsatisfiableError`/`NoSubstituteError` in `src/core/types.ts`, one class per generator invariant) more closely than a single parameterised class would, and lets every test in the chunk's own list ("assert the *specific* error thrown, not just that something did") do a plain `.toThrow(SomeSpecificError)` rather than inspecting a field on a generic one. | `src/core/coach/applyProposal.ts` |
| 2026-09-02 | Chunk 28: `adjust_load`'s domain ceilings — an RPE cap per `BlockKind` (primer/down-regulate 4, main 8.5, secondary 8, superset/finisher 9) and a `%TM` cap of **0.88** on `main` only | `01-METHODOLOGY.md §3.2` only states T1's own cap in prose ("RPE cap 8.5"); §3.3/§3.4 give T2/T3 their RPE ranges but no single number, and no other tier is ever percent-based in a generated program at all. RPE ceilings taken straight from each block's own §3 prose (T2 "RPE 7-8" → 8; T3 "RPE 7-9" → 9); the finisher (§3.5) has no RPE figure of its own and was given the accessory-superset ceiling as the closest defined analogue rather than left unbounded. The `%TM` ceiling uses **0.88**, not the doc's rounder "87%" prose figure — `src/core/progression/waves.ts`'s own `WAVE_6` peak-week top set is the actual highest percent the generator itself will ever emit, and a proposal must never exceed what the generator itself would prescribe, not a rounded description of it. | `src/core/coach/applyProposal.ts` |
| 2026-09-02 | Chunk 28: `swap_exercise` and `adjust_load` **can** target a `main` block; only `adjust_sets` is refused there | `docs/11-COACH-PLATFORM.md §4`'s narrative summary says generally "target block is never kind: 'main'", but the chunk file's own numbered validation rules (§2 rule 3) and tool-schema section (§5) both scope the main-block refusal to `adjust_sets` specifically — the "never trim T1" rule (`01-METHODOLOGY.md §1.3`), not a blanket "never touch T1" one. §5's own text for `adjust_load` ("every tier has its own honest ceiling... a T1 main-lift set never goes past 88% training max or RPE 8.5") only makes sense if `adjust_load` can reach a main block at all. Followed the specific, executable chunk file over the looser earlier narrative section it says exists "so 25's chat and 27's debrief don't paint themselves into a shape that can't hold them" — not a rule this chunk itself restates precisely. | `src/core/coach/applyProposal.ts`, `docs/11-COACH-PLATFORM.md §4` (narrative left as-is, this row is the correction) |
| 2026-09-02 | Chunk 28: a `swap_exercise` never recomputes `weightKg`/`percentTm` against the new exercise's own training max — the swapped-in exercise keeps exactly the sets the old one had | `applyProposal`'s signature, as given in the chunk file, takes `ctx: LibraryContext` (equipment/pain/complexity) with no training maxes in scope — recomputing a weight would need them. For the common case (an accessory swap, RPE-targeted, no `weightKg` at all) this is exactly right. For a main-lift swap between two barbell lifts with different 1RMs, the swapped exercise's sets keep the *original* lift's numbers — a known, documented scope limit of this chunk's given signature, not an oversight; a future chunk that wants a training-max-aware swap would need to widen what's passed in. | `src/core/coach/applyProposal.ts` |
| 2026-09-02 | Chunk 28: `adjust_load` changing `percentTm` clears the set's `weightKg` (falls back to the player's existing RPE-only display); changing only `rpe` leaves `weightKg`/`percentTm` untouched | The same reasoning `resolvePercentTmSet`'s "no training max on file" fallback already uses — a stale `kg` computed against the *old* percent is actively wrong once the percent changes, and `SetRow.tsx` already renders "RPE X" whenever `weightKg` is absent, so there is a real, already-built fallback to drop into rather than showing a number that no longer matches its own stated percent. | `src/core/coach/applyProposal.ts` |
| 2026-09-02 | Chunk 28: `adjust_sets` against a block with `rounds` set (a superset, or the rounds-based primer) resizes **every** exercise in that block and updates `rounds` itself, even though the tool call only ever names one `slot` | `materializeDay`/the generator both give every exercise in such a block the same set count, alternating by round — resizing only the named slot would desync the block (`D1` at 4 sets, `D2` still at 3). Not named explicitly in the chunk file's own rule list, but follows directly from the domain invariant every other producer of `SessionBlock[]` in this repo already respects. | `src/core/coach/applyProposal.ts` |
| 2026-09-02 | Chunk 28: new `src/core/coach/proposalTargets.ts` — not named in the chunk file's own file list | `propose_change` needs a real `sessionId`/`blockLetter`/`slot`/`exerciseId` to target, and chunk 25's `buildCoachContext` (frozen, its own tests unchanged) carries none of that — only a per-week session *count* and status. Rather than widen that function's shape (and its existing tests) for a chunk-28-only need, a second, additive function turns this week's `planned` sessions into an addressable listing appended to the same system prompt, reusing the `thisWeeksSessions(sessions)` `sendCoachMessage` already computes — no extra query. | `src/core/coach/proposalTargets.ts`, `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 28: `sendCoachMessage`'s `coachCompletion` call moved from `kind: 'chat'` (haiku) to `kind: 'proposal'` (sonnet) for **every** chat turn, not only ones that end up producing a proposal | `docs/11-COACH-PLATFORM.md §2` prices "the tool-calling turn" at sonnet and §7 rules out a cheap first pass that decides whether to "upgrade" (one call in, one call out — no second, cheaper call to check intent first). Once `tools` is attached to every turn so the athlete can ask for a change at any point in the conversation, every turn *is* the tool-calling turn by construction. A real, deliberate cost increase for plain chat, not an oversight — `MODEL_FOR_KIND.chat` (haiku) stays in `anthropic.ts`'s map for `generateSessionDebrief`'s own unrelated `'debrief'` kind and any future direct caller. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 28: `applyCoachProposal`/`dismissCoachProposal` both check `isCoachConfigured()` first, even though neither ever calls `coachCompletion` | The chunk file's own snippet for `applyCoachProposal` doesn't mention this check, but `docs/11-COACH-PLATFORM.md §6.1` says it plainly for "every coach action," and `generateSessionDebrief`'s cache-hit path (also model-call-free) already checks it unconditionally, first — matched that precedent rather than reasoning a model-call-free action is exempt. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 28: `applyCoachProposal` revalidates `TAGS.program`/`TAGS.sessions`/`TAGS.coach` (via `revalidateTag`), not the chunk file's own illustrative `revalidatePath('/program')`/`revalidatePath('/session/[id]', 'page')` snippet | The chunk file itself said to check `updateLiveProgram`'s real revalidation calls and match them — that function (and every mutation in the top-level `src/server/actions.ts`) uses `revalidateTag` exclusively; `revalidatePath` appears nowhere in that file. Matched the real, live convention over the chunk file's own inline sketch of it. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 28: the athlete's equipment/complexity/pain-flag `LibraryContext` is re-derived inline in `src/server/coach/actions.ts` (three lines, mirroring `buildProgram`'s own derivation in the top-level `actions.ts`) rather than imported from there | The top-level `src/server/actions.ts` is a `'use server'` module — importing any value from it (even just a helper) registers every one of its exports as a callable worker on this module's own callers too, the exact action-isolation risk `src/server/coach/result.ts`'s own doc comment already calls out for not importing that file's `Result` type. Three duplicated lines is cheaper than that coupling. | `src/server/coach/actions.ts` |
| 2026-09-02 | Chunk 28: `ProposalCard` renders **alongside** the plain chat bubble, not in its place — both the model's own explanatory sentence and the structured diff show together | The chunk file left this "your call." The model's own sentence often carries real color (why it's suggesting the change) beyond what the diff's numbers alone say; showing both costs nothing extra (the reply text was going to be saved and rendered either way) and a tool-call-only reply with no text gets a plain "Here's a proposed change:" filler line rather than an empty bubble. | `src/app/coach/page.tsx`, `src/components/coach/ProposalCard.tsx` |
